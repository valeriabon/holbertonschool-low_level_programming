#include <stdio.h>
#include <string.h>

/**
 * main - This C program use printf to print
 * Return: zero
 */
int main(void)
{
	printf("%s \n", "with proper grammar, but the outcome is a piece of art,");
	return (0);
}
